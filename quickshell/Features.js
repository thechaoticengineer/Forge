// Pure helpers for the panel's feature-spec list (M1: format and discovery).
// Stubbed pending the read-only discovery API and panel overlay.

function featureRows(response) {
    return []
}

function editorCommand(feature) {
    return []
}
