# Forge

A minimal wrapper around AI coding agents (Claude Code, Codex CLI).

The goal: the fastest possible loop for Forge to work on Forge itself —
pick a repo, state a goal, let the agent work, review the diff, merge.

Starting from scratch. The previous orchestrator implementation lives in
git history before this commit.
