<!-- 
This folder holds UI mockups and diagrams for this feature.

Mockups are created with pen.dev (*.pen files), each exported to a PNG with the same base name (e.g., feature-dialog.pen and feature-dialog.png).

Diagrams can be created as Mermaid files (*.mmd) or inline Mermaid blocks in Markdown (```mermaid ... ```).

Once design files are added, this note can be removed.
-->
